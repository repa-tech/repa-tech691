# Netlify détecte Next.js et applique l’adaptateur OpenNext automatiquement.
[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"

[dev]
  command = "npm run dev"
  port = 3000
  autoLaunch = false
